<%= form_for(resource, url: resources_path(resource_type: resource.resourceable_type, resource_id: resource.id), remote: true, format: :json, id: 'submit-resource') do |form| %>
  <%= form.hidden_field :resourceable_type %>
  <%= form.fields_for :resourceable do |form| %>

    <div class="form-group">
      <%= form.label :url, 'Book URL', class: 'd-block' %>
      <%= form.text_field :url, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= form.label :title, 'Book Title', class: 'd-block' %>
      <%= form.text_field :title, class: 'form-control' %>
    </div>

    <%= render partial: 'form_field_image_or_image_url', locals: { form: form } %>
    <%= render partial: 'form_field_authors', locals: { form: form } %>
  <%- end %>

  <div class="actions">
    <%= form.submit 'Submit Book', class: 'btn btn btn-rect-lg push-button btn-secondary' %>
  </div>
<% end %>
